version: '3.8'
services:
  web:
    build:
      context: .
      target: dev
    volumes:
      - .:/src
    ports:
      - '5000:5000'
    environment:
      - NODE_ENV=development
    command: npm run dev
